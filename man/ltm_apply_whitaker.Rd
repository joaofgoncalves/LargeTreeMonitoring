% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LTM_pre_proc.R
\name{ltm_apply_whitaker}
\alias{ltm_apply_whitaker}
\title{Apply Whittaker smoothing to a regularized \code{spidf} time series}
\usage{
ltm_apply_whitaker(
  spidf_obj,
  lambda = 5000,
  quantile_thresh = 0.35,
  use_weights = TRUE,
  min_weight = 0.01
)
}
\arguments{
\item{spidf_obj}{A regularized object of class \code{spidf}, typically returned
by \code{\link[=ltm_regularize_spidf]{ltm_regularize_spidf()}}.}

\item{lambda}{Numeric smoothing parameter passed to \code{\link[phenofit:whit2]{phenofit::whit2()}}.}

\item{quantile_thresh}{Numeric probability used to compute the weighting
threshold when \code{use_weights = TRUE}.}

\item{use_weights}{Logical scalar. If \code{TRUE}, values greater than or equal to
the \code{quantile_thresh} quantile receive weight \code{1}, lower non-missing values
receive \code{min_weight}, and missing values receive \code{0}.}

\item{min_weight}{Numeric weight assigned to finite values below the
threshold when \code{use_weights = TRUE}.}
}
\value{
An object of class \code{spidf} with \code{spi_smooth}, optional \code{spi_weight},
and optional \code{spi_mov_smooth} columns, plus updated Whittaker-smoothing
attributes.
}
\description{
Smooths the daily \code{spi} series with \code{\link[phenofit:whit2]{phenofit::whit2()}}. When moving-window
values are present, the function also smooths \code{spi_mov_wind} into
\code{spi_mov_smooth}. Optional weights can reduce the influence of lower
spectral-index values before smoothing the main \code{spi} series.
}
\seealso{
Other preprocessing helpers: 
\code{\link{ltm_apply_moving_quantile}()},
\code{\link{ltm_regularize_spidf}()}
}
\concept{preprocessing helpers}

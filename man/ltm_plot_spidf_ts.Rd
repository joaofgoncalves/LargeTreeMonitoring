% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LTM_plots.R
\name{ltm_plot_spidf_ts}
\alias{ltm_plot_spidf_ts}
\title{Plot an \code{spidf} time series and detected breaks}
\usage{
ltm_plot_spidf_ts(
  spidf_obj,
  tree_id = "",
  df_breaks = NULL,
  only_valid_breaks = TRUE,
  valid_breaks_mode = "any",
  validators_sel = NULL
)
}
\arguments{
\item{spidf_obj}{An object of class \code{spidf}.}

\item{tree_id}{Optional tree identifier shown in the plot subtitle when not
empty and not \code{"---"}.}

\item{df_breaks}{Optional data frame of break-detection results, typically
produced by \code{\link[=as.data.frame.ts_breaks]{as.data.frame.ts_breaks()}}.}

\item{only_valid_breaks}{Logical scalar. If \code{TRUE}, plot only breaks that
pass at least one selected validator or all selected validators according
to \code{valid_breaks_mode}.}

\item{valid_breaks_mode}{Character scalar. Either \code{"any"} to retain breaks
passing any selected validator or \code{"all"} to retain breaks passing all
selected validators.}

\item{validators_sel}{Optional character vector selecting validator groups
used when \code{only_valid_breaks = TRUE}. Supported values are \code{"lt"},
\code{"st"}, and \code{"st_trend"}. When \code{NULL}, all available validator columns are
considered.}
}
\value{
A \code{ggplot} object.
}
\description{
Builds a \code{ggplot} visualization of available spectral-index series stored in
an \code{spidf} object. The plot can overlay break dates from a flat
break-detection data frame and optionally restrict them to validator-approved
breaks.
}
\seealso{
Other plotting helpers: 
\code{\link{plot_valid_breaks}()}
}
\concept{plotting helpers}

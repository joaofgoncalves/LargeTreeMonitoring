% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LTM_pre_proc.R
\name{ltm_apply_moving_quantile}
\alias{ltm_apply_moving_quantile}
\title{Apply a moving quantile to a regularized \code{spidf} time series}
\usage{
ltm_apply_moving_quantile(spidf_obj, quant = 0.95, win_size = 9)
}
\arguments{
\item{spidf_obj}{A regularized object of class \code{spidf}, typically returned
by \code{\link[=ltm_regularize_spidf]{ltm_regularize_spidf()}}.}

\item{quant}{Numeric scalar probability passed to \code{\link[stats:quantile]{stats::quantile()}}.}

\item{win_size}{Integer window size in days. The window is split around each
observation using \code{floor(win_size / 2)} days before and
\code{ceiling(win_size / 2)} days after.}
}
\value{
An object of class \code{spidf} with an added \code{spi_mov_wind} column and
updated attributes \code{mov_window}, \code{mov_window_quantile}, and
\code{mov_window_size_days}.
}
\description{
Computes a centered moving quantile over the daily \code{spi} column of a
regularized \code{spidf} object. The result is stored in \code{spi_mov_wind}; existing
metadata are preserved and moving-window attributes are updated.
}
\seealso{
Other preprocessing helpers: 
\code{\link{ltm_apply_whitaker}()},
\code{\link{ltm_regularize_spidf}()}
}
\concept{preprocessing helpers}

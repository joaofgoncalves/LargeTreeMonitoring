% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LTM_pre_proc.R
\name{ltm_regularize_spidf}
\alias{ltm_regularize_spidf}
\title{Regularize an \code{spidf} time series to daily observations}
\usage{
ltm_regularize_spidf(spidf_obj, method = "linear", use_cloud_mask = TRUE, ...)
}
\arguments{
\item{spidf_obj}{An object of class \code{spidf}.}

\item{method}{Character scalar interpolation or imputation method. Supported
values are \code{"linear"}, \code{"spline"}, \code{"stine"}, \code{"mean"}, \code{"kalman"},
\code{"locf"}, and \code{"nocb"}.}

\item{use_cloud_mask}{Logical scalar. If \code{TRUE}, interpolate the
cloud-masked values in \code{masked_vals}; if \code{FALSE}, interpolate the raw
spectral-index values in \code{spi}.}

\item{...}{Additional arguments passed to the selected \pkg{imputeTS}
imputation function.}
}
\value{
An object of class \code{spidf} with one row per day, columns \code{ti}, \code{spi},
\code{original_spi}, \code{masked_vals}, and \code{cloud_mask}, and updated attributes
\code{regularized = TRUE} and \code{regularize_method = method}.
}
\description{
Converts an irregular Sentinel-2 spectral-index time series to a daily
sequence spanning the available data range and imputes missing daily values
with a selected method from \pkg{imputeTS}. The output keeps the \code{spidf}
metadata from the input and records the regularization status in attributes.
}
\seealso{
Other preprocessing helpers: 
\code{\link{ltm_apply_moving_quantile}()},
\code{\link{ltm_apply_whitaker}()}
}
\concept{preprocessing helpers}

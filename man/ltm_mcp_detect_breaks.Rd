% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LTM_break_detection.R
\name{ltm_mcp_detect_breaks}
\alias{ltm_mcp_detect_breaks}
\title{Detect breakpoints using Bayesian change-point models}
\usage{
ltm_mcp_detect_breaks(
  spidf,
  ts_name = "spi",
  season_adj = TRUE,
  s_window = 30,
  thresh_date,
  sample = "both",
  n_chains = 3,
  n_cores = 3,
  n_adapt = 500,
  n_iter = 1000,
  downsample = NULL,
  lt_window = NULL,
  lt_thresh_change = -10,
  lt_fun = median,
  st_window = NULL,
  st_thresh_change = -10,
  st_fun = median,
  trend_window = NULL,
  trend_require_lower_level = TRUE,
  trend_rand_B = 99,
  trend_rand_seed = 11235,
  trend_post_pct_thresh = -10,
  trend_alpha = 0.1,
  trend_deficit_tol = 0.05,
  trend_min_prop_below = 0.6,
  trend_avg_deficit_thresh = -1.5,
  ...
)
}
\arguments{
\item{spidf}{An object of class \code{spidf}.}

\item{ts_name}{Character. Name of the time-series column to analyze. Must be
one of \code{VALID_DATA_TYPES}.}

\item{season_adj}{Logical. If \code{TRUE}, applies seasonal adjustment before
breakpoint detection when the series is long enough.}

\item{s_window}{Integer. Seasonal window used by \code{stats::stl()} when
\code{season_adj = TRUE}.}

\item{thresh_date}{Date or date-like value. The selected breakpoint must
occur on or after this date to be considered valid.}

\item{sample}{Character scalar passed to the \code{sample} argument of
\code{\link[mcp:mcp]{mcp::mcp()}}.}

\item{n_chains}{Integer number of MCMC chains passed to \code{\link[mcp:mcp]{mcp::mcp()}}.}

\item{n_cores}{Integer number of cores passed to \code{\link[mcp:mcp]{mcp::mcp()}}.}

\item{n_adapt}{Integer adaptation length passed to \code{\link[mcp:mcp]{mcp::mcp()}}.}

\item{n_iter}{Integer iteration count passed to \code{\link[mcp:mcp]{mcp::mcp()}}.}

\item{downsample}{Optional positive integer. When supplied, the input series
is sampled every \code{downsample} rows before model fitting.}

\item{lt_window}{Optional integer. If provided, long-term validation is
computed using symmetric windows of this size around the break. Use \code{NULL}
or \code{0} to use all observations before and after the break.}

\item{lt_thresh_change}{Numeric. Maximum allowed percent change for the
long-term validator. More negative values indicate stronger declines.}

\item{lt_fun}{Function. Aggregation function used by the long-term validator,
typically \code{median} or \code{mean}.}

\item{st_window}{Optional integer. Half-window size for the short-term
validator. If \code{NULL}, short-term validation is skipped.}

\item{st_thresh_change}{Numeric. Maximum allowed percent change for the
short-term validator.}

\item{st_fun}{Function. Aggregation function used for the short-term
validator.}

\item{trend_window}{Optional integer. Half-window size for the randomized
short-term trend validator. If \code{NULL}, trend validation is skipped.}

\item{trend_require_lower_level}{Logical. If \code{TRUE}, the post-break mean must
be lower than the pre-break mean in the trend validator.}

\item{trend_rand_B}{Integer. Number of randomized pre-break windows used to
build the null distribution in the trend validator.}

\item{trend_rand_seed}{Optional integer seed for reproducible randomized
trend validation.}

\item{trend_post_pct_thresh}{Numeric. Percent-slope threshold used by the
trend validator.}

\item{trend_alpha}{Numeric. One-sided p-value threshold used by the trend
validator.}

\item{trend_deficit_tol}{Numeric. Relative tolerance used to define
below-baseline post-break observations in the trend validator.}

\item{trend_min_prop_below}{Numeric. Minimum proportion of post-break values
that must remain below baseline for depression-based trend validation.}

\item{trend_avg_deficit_thresh}{Numeric. Maximum allowed mean post-break
deficit, in percent, for depression-based trend validation.}

\item{...}{Additional arguments reserved for future use.}
}
\value{
An object of class \code{ts_breaks_run} with method \code{"mcp"}. In addition
to the shared break and validator fields, the object includes MCP-specific
\code{pars} and \code{uncertainty_diag} entries when parameter summaries are
available.
}
\description{
Fits a two-segment intercept-only model with \code{\link[mcp:mcp]{mcp::mcp()}} to a selected
\code{spidf} time-series column and returns one \code{ts_breaks_run} object. The model
is fit against a seasonally adjusted series when \code{season_adj = TRUE} and the
series is long enough for STL decomposition.
}
\details{
The wrapper uses internally defined priors based on empirical quantiles and
standard deviation of the analysis series. Extra arguments in \code{...} are
passed to \code{\link[mcp:mcp]{mcp::mcp()}} after resolving deprecated LargeTreeMonitoring aliases
\code{tresh_int} and \code{thresh_change}.

Fits a Bayesian one-change-point step model to a preprocessed analysis series
using mcp::mcp(). The analysis series is obtained by optional NA interpolation
followed by optional STL-based seasonal adjustment. The fitted mcp model is
list(ysa ~ 1, ~ 1) with par_x = "di", implying two constant-mean segments
separated by one estimated changepoint. Priors are data-dependent and
directional, favoring a decrease from the first segment level to the second.

Let \eqn{y_i} denote the raw input series at ordered observations
\eqn{i = 1, \ldots, n}. Missing values are deterministically interpolated
before model fitting; denote the interpolated series by \eqn{\tilde{y}_i}.
If seasonal adjustment is enabled and feasible, the fitted series is
\eqn{z_i = SA(\tilde{y}_i)}, where \eqn{SA(\cdot)} denotes STL-based seasonal
adjustment. Otherwise, \eqn{z_i = \tilde{y}_i}.

The MCP specification \code{list(ysa ~ 1, ~ 1)} with \code{par_x = "di"} defines a
one-change-point Gaussian step model on \eqn{z_i}:

\deqn{
z_i \mid c, \alpha_1, \alpha_2, \sigma \sim \mathrm{Normal}(\mu_i, \sigma^2)
}

\deqn{
\mu_i = \alpha_1 I(i < c) + \alpha_2 I(i \ge c), \qquad i = 1, \ldots, n
}

where \eqn{c = cp_1} is the break index, \eqn{\alpha_1 = int_1} is the
pre-break level, and \eqn{\alpha_2 = int_2} is the post-break level.

The wrapper uses empirical, data-dependent priors:

\deqn{
\alpha_1 \sim \mathrm{Normal}(q_{0.75}, s^2)\ \text{truncated to}\ [0, 1]
}

\deqn{
\alpha_2 \sim \mathrm{Normal}(q_{0.25}, s^2)\ \text{truncated to}\ [0, \alpha_1]
}

\deqn{
c \sim \mathrm{Uniform}(1, n)
}

where \eqn{q_{0.75}} and \eqn{q_{0.25}} are the empirical 75th and 25th
percentiles of the analysis series and \eqn{s} is its empirical standard
deviation.

This prior specification encodes a directed alternative in which the
post-break level cannot exceed the pre-break level. Accordingly, the model is
designed to detect downward level shifts rather than arbitrary changes in
mean level.
}
\seealso{
Other break-detection wrappers: 
\code{\link{ltm_bfast01_detect_breaks}()},
\code{\link{ltm_cpm_detect_breaks}()},
\code{\link{ltm_strucchange_detect_breaks}()},
\code{\link{ltm_wbs_detect_breaks}()}
}
\concept{break-detection wrappers}
